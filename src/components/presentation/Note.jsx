export function Note({ children }) {
  // Note content is extracted by Slide component and displayed in Demo header
  // This component doesn't render anything
  return null;
}

Note.displayName = "Note";
